# shared/components/ — shared presentational bits

Small, generic React components reused across features.

| File | Role |
|------|------|
| `DiceIcon.tsx` | Die-face icon used by the dice roller and attack/roll UIs. |

Larger look-preserving primitives (panels, modals) live in [`../../ui/`](../../ui/).
